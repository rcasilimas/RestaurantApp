class Category {
    constructor(id, categoryId, title, imageUrl) {
        this.id = id;
        this.categoryId = categoryId;
        this.title = title;
        this.imageUrl = imageUrl;
    }
}

export default Category;